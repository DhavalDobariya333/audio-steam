# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — Python dependencies for the Audio Stream Server
# ──────────────────────────────────────────────────────────────────────────────
# Install: pip install -r requirements.txt
#
# fastapi      — ASGI web framework (HTTP + WebSocket)
# uvicorn      — ASGI server to run FastAPI
# websockets   — WebSocket protocol implementation (used by uvicorn)
#
# NOTE: We pin major versions for stability on Termux but allow patch updates.
# ──────────────────────────────────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
websockets>=14.0,<15.0
